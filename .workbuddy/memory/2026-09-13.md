# 2026-09-13

## 为 Hugo Solitude 适配 FCircle 友链抓取

用 grill-me 走完 5 个分支决策后实施（用户全选推荐项）：

- 交付形态：只改本地 + 留档（暂不提上游 PR）
- 数据源：只用 `LINK`（爬渲染后 HTML），不启用 `SETTINGS_FRIENDS_LINKS`
- theme 名：`hugo-solitude`；URL：`https://blog.518339.xyz/links/`
- `fc_settings.yaml`：保留示例注释 + 插入真实条目 + 补 `hugo-solitude` 说明

### 改动文件
- `css_rules.yaml`：新增 `link_page_rules.hugo-solitude`（插在 `stellar` 后、`anzhiyu` 前），+15 行
- `fc_settings.yaml`：`LINK` 首次启用；补 `hugo-solitude` 注释；补 YAML `&` 转义警告
- `CHANGELOG.md`：顶部加 `Unreleased` 段（🆕 新增规则）
- `tools/verify_css_rules.py`：新增，规则实测校验脚本

### 实测结果
`tools/verify_css_rules.py hugo-solitude https://blog.518339.xyz/links/` → 三字段各命中 3 条，校验通过：

| author | link | avatar |
|---|---|---|
| 伍拾柒 | https://www.efu.me/ | https://github.com/everfu.png |
| 张洪Heo | https://blog.zhheo.com/ | t3.gstatic.com/faviconV2?...&size=128 |
| 清羽飞扬 | https://blog.liushen.fun/ | https://blog.liushen.fun/info/avatar.ico |

墓碑站「示例失联站点」未出现在 `.cf-friends-name` 中 → 符合预期。

### 待办 / 后续
- 规则需在真实 FCircle 运行中跑一段时间，稳定后可考虑提 PR 给上游 `Rock-Candy-Tea/hexo-circle-of-friends`。
- 用户确认：墓碑站不抓；张洪Heo 头像按 `links.yaml` 原值（不改成 `img.zhheo.com` 那个）。
